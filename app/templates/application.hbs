{{#burger-menu as |burger|}}
  {{#burger.menu itemTagName="li" as |menu|}}
    <button {{action burger.state.actions.close}}>Close</button>

    <ul>
      {{#menu.item}}
        {{link-to "Página inicial" "index"}}
      {{/menu.item}}
    </ul>
  {{/burger.menu}}

  {{#burger.outlet}}
    <div class="app">
      <header class="header">
        <button {{action burger.state.actions.toggle}} class="btnmenu">Menu</button>
        <img src="assets/images/logo-v2v.png" alt="Logo V2V" class="logo">
      </header>

      <main class="main">
        <div class="content">
          {{outlet}}
        </div>

        <footer class="footer">
          <small>
            &copy; Todos os direitos reservados - V2V.net
          </small>
        </footer>
      </main>
    </div>
  {{/burger.outlet}}
{{/burger-menu}}